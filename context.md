Overview:
I need to build an expenses tracking app for my girlfriend and I. The app will be hosted on Vercel and should be minimalistic in nature. Features are described in the next section

Features:

1. When the app is loaded, it should diplay 2 options: 'Irvan' and 'Isobel'. These represent two users, namely my girlfriend and I. For each user, the following functionalities should be added:

2. Each user should be able to add an expense. For each expense that is added, a mandatory description should be added. For each entry, the app should provide the user with an emoji option. You might have to manually create these emojis. I need an emoji for restaurant, grocery, visits, train tickets, miscelleneaous. The app should automatically apend the date and time to each entry. 

3. Each user should be able to view the total expenses and their share of the expenses
4. Each user should be able to remove only the expenses they added 

Theme:

1. The theme should be minimalistic and not like those overwhelming vibe coded apps
2. Use a light background and a black to write
3. Represent 'Irvan' as a cartoon boy and 'Isobel' as a cartoon like. Look at @example.png to get an example of the type of vector graphics I am expecting 
4. The expenses should be displayed in an old cashier register
5. Whenever expenses are added, this figure in the old cashier register should update as an expense is added. It should be updated in an old style fashion as if the thing is rolling
6. This app will be mainly used from iphone browser. so ensure it is a mobile friendly web app 


Additional details:
I will deploy on vercel