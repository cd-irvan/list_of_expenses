# Created automatically when you add the Upstash Redis (Vercel KV) integration
# in your Vercel project settings. For local dev, fill these in or skip them
# and the app will use a local JSON file at .data/expenses.json instead.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
